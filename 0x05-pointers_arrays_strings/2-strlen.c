#include "holberton.h"

/**
 * _strlen - Returns the length of a string.
 * @s: String.
 *
 * Return: Length of @s.
 */
int _strlen(char *s)
{
	int length = 0;

	while (*str++)
		length++;

	return (length);
}
